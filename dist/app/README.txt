flowexch.apk — Capacitor Android debug build of FlowExch.

Rebuild: npm run build:apk
(Requires .tools/jdk-17 and .tools/android-sdk)

Download URL: /app/flowexch.apk (set VITE_ANDROID_APK_URL in .env)
